import { useNavigate } from "react-router-dom";

// TODO ::
// 1. user board list + user name, logout
// 2. update user data, deleteUser
// * Profile image => drop down...

const Profile = () => {
  let navigate = useNavigate();
  return <></>;
};

export default Profile;
